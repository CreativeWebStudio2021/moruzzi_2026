<style>
	.contact-extra-block {
		margin-top: 48px;
		padding-top: 32px;
		border-top: 1px solid #e5e5e5;
		scroll-margin-top: 120px;
	}

	.contact-extra-block__title {
		margin: 0 0 16px;
		font-family: 'Inria Serif', serif;
		font-size: clamp(28px, 4vw, 36px);
		font-style: italic;
		line-height: 1.2;
		color: var(--red);
	}

	.contact-extra-block__intro,
	.contact-extra-block__text {
		margin: 0 0 16px;
		font-size: 16px;
		line-height: 1.6;
		color: var(--black);
		max-width: 900px;
	}

	.contact-extra-block__actions {
		margin: 24px 0 12px;
	}

	.contact-extra-block__note {
		margin: 0;
		font-size: 14px;
	}

	.contact-extra-block__note a {
		color: var(--red);
		text-decoration: underline;
	}

	.contact-hours-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 24px;
		margin-top: 8px;
	}

	.contact-hours-card {
		background: #fff;
		border: 1px solid #e8e8e8;
		border-radius: 8px;
		padding: 20px;
	}

	.contact-hours-card__title {
		margin: 0 0 8px;
		font-size: 18px;
		font-weight: 700;
		color: var(--black);
	}

	.contact-hours-card__period {
		margin: 0 0 16px;
		font-size: 14px;
		color: #666;
		line-height: 1.4;
	}

	.contact-hours-table {
		width: 100%;
		border-collapse: collapse;
		font-size: 15px;
	}

	.contact-hours-table th,
	.contact-hours-table td {
		padding: 8px 0;
		border-bottom: 1px solid #f0f0f0;
		vertical-align: top;
		text-align: left;
	}

	.contact-hours-table th {
		width: 38%;
		font-weight: 700;
		color: var(--black);
	}

	.contact-hours-table td {
		color: #333;
	}

	.contact-ebay-btn {
		display: inline-flex;
		width: auto;
		min-width: 0;
		padding-left: 36px;
		padding-right: 36px;
		text-decoration: none;
		border: none;
	}
</style>
